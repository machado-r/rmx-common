export default (theme) => ({
  button: {
    marginLeft: '0.5em',
  },

  cancelarOuSalvar: {
    marginBottom: '1em',
    display: 'flex',
    justifyContent: 'flex-end',
  },

  end: {
    marginBottom: '1em',
    display: 'flex',
    justifyContent: 'flex-end',
    marginRight: theme.spacing(1),
  },
});
